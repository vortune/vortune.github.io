# Switching the Default Version of Python

If you need to execute `Python3` when you run the `python` command, the easiest way to do it is create an alias like this:

```bash
alias python=python3
```

